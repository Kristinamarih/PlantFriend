<h1><%= @product.name %></h1>
<%= link_to @product.category.name, product_category_path(@product.id, @product.category.id) %>

<h3>Grown by: <%= @product.user.username %></h3>

<% if @product.user != current_user %>
    <p><%= link_to "Message me!", conversations_path(sender_id: current_user.id, recipient_id: @product.user.id), method: 'post' %>
<% end %>

<div>Quantity: <%= @product.quantity %></div>
<div>Price: <%= @product.price %></div>
<div>Description: <%= @product.description %></div>

<% if @product.user == current_user %>
    <%= link_to "Edit product", edit_product_path(@product) %>
    <%= link_to "Delete product", product_path(@product), :data => {:confirm => "You sure?", :method => "delete"} %>
<% end %>

<h4>Comments:</h4>

<p><%= link_to "See all comments", product_comments_path(@product) %></p>
<p><%= link_to "Add a new comment", new_product_comment_path(@product) %></p>

<% @product.comments.each do |c| %>
    <li><strong><%= c.user.username %></strong> says "<%= link_to c.content, product_comments_path(@product, c)%>"</li>
<% end %>

<% if @product.comments.empty? %>
    <p>Be the first to leave a comment!</p>
<% end %>